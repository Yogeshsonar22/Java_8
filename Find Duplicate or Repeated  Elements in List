
class Codechef
{
    public static void main(String[] args) throws java.lang.Exception
    {
        // your code goes here
        List < String > list = Arrays.asList("Yogesh", "sonar", "yogesh", "sOnAR", "Ajay");

        Set < String > set = new HashSet < >();
        
        list.stream()   .map(String::toLowerCase) .filter(x -> !set.add(x)).forEach(System.out::println);
    }
}
// Your Output
// yogesh
// sonar

OR 

class Codechef
{
	public static void main (String[] args) throws java.lang.Exception
	{
String str = "programming";

   Character ch= str.chars().mapToObj(c-> (char)c).collect(Collectors.groupingBy(c->c, LinkedHashMap::new, Collectors.counting())).entrySet().stream().filter(e->e.getValue()==2).map(e-> e.getKey()).findFirst().get();
    System.out.println(ch);
	}
